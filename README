Welcome to the NPS Bloom Filter package.

This package depends on an old release of automake.

in SVN, must be auto toolchained before can do ./configure

libtoolize
aclocal
autoheader
automake-1.11 --add-missing
autoconf

Then

./configure
make

You will need to have the following packages installed to use this program:

  apt-get -y install make gcc g++ libssl-dev

